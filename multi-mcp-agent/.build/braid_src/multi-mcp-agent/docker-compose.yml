services:
  alphavantage-mcp:
    build:
      context: ./mcp/alphavantage
      dockerfile: Dockerfile
    container_name: alphavantage-mcp-server
    environment:
    - ALPHAVANTAGE_API_KEY=${ALPHAVANTAGE_API_KEY}
    restart: unless-stopped
    networks:
    - braid-mcp-network
    healthcheck:
      test: &id001
      - CMD
      - curl
      - -f
      - http://localhost:8080/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    volumes:
    - alphavantage-data:/app/data
  perplexity-mcp:
    build:
      context: ./mcp/perplexity
      dockerfile: Dockerfile
    container_name: perplexity-mcp-server
    environment:
    - PERPLEXITY_API_KEY=${PERPLEXITY_API_KEY}
    restart: unless-stopped
    networks:
    - braid-mcp-network
    healthcheck:
      test: *id001
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    volumes:
    - perplexity-data:/app/data
networks:
  braid-mcp-network:
    driver: bridge
    name: braid-mcp-network
volumes:
  alphavantage-data:
    driver: local
  perplexity-data:
    driver: local
